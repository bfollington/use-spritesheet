{
    "private": true,
    "workspaces": ["example", "use-spritesheet"],
    "scripts": {
        "build": "cp README.md use-spritesheet/README.md && cd use-spritesheet && yarn build && cd ../example && yarn build"
      }
}